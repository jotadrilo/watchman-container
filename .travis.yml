language: bash
sudo: required
services:
  - docker
before_install:
  - sudo apt-get -qq update
  - sudo apt-get install -y jq
install: true
script: bash build && bash test
deploy:
  provider: script
  script: bash push
  on:
    branch: master
